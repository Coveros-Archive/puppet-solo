#
# Hiera values for global server values
# 
# To edit the encrypted values:
# sudo eyaml edit --pkcs7-private-key=/etc/puppet/secure/keys/private_key.pkcs7.pem --pkcs7-public-key=/etc/puppet/secure/keys/public_key.pkcs7.pem  hiera/global.yaml
# 

my_property: 'global property value'

# Encrypted property
# General format(delete the spaces): DEC :: PKCS7 [SomeValue]!
secure_property: DEC::PKCS7[EncryptedValue]!